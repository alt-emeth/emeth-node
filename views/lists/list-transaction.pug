mixin my-transaction(txId, time, addrFrom, addrTo)
  include ../helpers/helper.js.pug
  .row
    .col-sm-4
      .d-flex.align-items-sm-center
        .d-none.d-sm-flex
          div.mr-1.circle Tx
        
        .d-flex.d-sm-block
          span(style={'white-space': 'nowrap'}).d-inline-block.d-sm-none TX#&nbsp;
          a(href=`/tx/${txId}` title='transaction id').nav-link.mb-0.mb-0.p-0.tag-transaction #{txId}
          span.d-none.d-sm-block.time #{time}
    .col-sm-8
      .d-flex.justify-content-between
        div(style={'max-width': '100%'})
          .transaction-addr
            span(style={'white-space': 'nowrap'}) From 
              a(href=`/address/${addrFrom}`) #{replaceAddress(addrFrom, addressPrefix)}
          .transaction-addr
            span(style={'white-space': 'nowrap'}) To 
              -if(transactions[i].target.type === 'token'){
                a(href=`/tokens/${addrTo.link}`) #{replaceAddress(addrTo.link, addressPrefix)}
              -} else if(transactions[i].target.type === 'store'){
                a(href=`/stores/${addrTo.link}`) #{replaceAddress(addrTo.link, addressPrefix)}
              -} else {
                a(href=`/address/${addrTo.link}`) #{replaceAddress(addrTo.link, addressPrefix)}
              -}
  hr

